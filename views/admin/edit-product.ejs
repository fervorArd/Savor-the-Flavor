<%- include ('../partials/messages'); %>
<h3>Edit a Product</h3>
<a href="/admin/products">
  <button type="button" class="btn btn-primary" name="button">Back to Products</button>
</a>
<div class="row mt-5">
  <div class="col-md-6 m-auto">
    <div class="card card-body">
      <form action="/admin/edit-product/<%= id  %>" method="POST">
        <div class="form-group">
          <label for="name">Title</label>
          <input
            type="text"
            id="title"
            name="title"
            class="form-control"
            value="<%= typeof title != 'undefined' ? title : '' %>"
          />
        </div>
        <div class="form-group">
          <label for="name">Description</label>
          <input
            type="textarea"
            id="description"
            name="description"
            class="form-control"
            value="<%= typeof description != 'undefined' ? description : '' %>"
          />
        </div>
        <div class="form-group">
          <label for="email">Category</label>
          <input
            type="text"
            id="category"
            name="category"
            class="form-control"
            value="<%= typeof category != 'undefined' ? category : '' %>"
          />
        </div>
        <div class="form-group">
          <label for="password">Price</label>
          <input
            type="text"
            id="price"
            name="price"
            class="form-control"
            value="<%= typeof price != 'undefined' ? price : '' %>"
          />
        </div>
        <div class="form-group">
          <label for="password2">Image</label>
          <input
            type="text"
            id="imagePath"
            name="imagePath"
            class="form-control"
            value="<%= typeof imagePath != 'undefined' ? imagePath : '' %>"
          />
        </div>
        <button type="submit" class="btn btn-primary btn-block">
          Submit
        </button>
      </form>
    </div>
  </div>
</div>
